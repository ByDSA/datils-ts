export type ArgPathOpt = { path?: string };

export type ArgSudoOpt = { sudo?: boolean };

export const ARG_SUDO_DEFAULT = false;

export type ArgDettachOpt = { dettach?: boolean };

export const ARG_DETTACH_DEFAULT = true;
