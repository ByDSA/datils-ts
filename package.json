{
  "name": "datils",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "test": "jest",
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc && npm run copyfiles",
    "copyfiles": "cp src/*.json dist"
  },
  "author": "Daniel Sales",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^14.0.27",
    "dotenv": "^10.0.0",
    "fs-extra": "^10.0.0",
    "typescript": "^3.9.7"
  },
  "devDependencies": {
    "@types/jest": "^26.0.8",
    "jest": "^29.5.0",
    "ts-jest": "^25.3.1"
  }
}
